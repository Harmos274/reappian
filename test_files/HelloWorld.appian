a!localVariables(
  local!helloWorld: "Hello World",
  local!isShowed: false,
  {
    a!button(
      label: local!helloWorld,
      showWhen: true,
      saveInto: {
        a!save(local!isShowed, true)
      }
    )
  }
)

